from fastapi import APIRouter

router = APIRouter(prefix="/api/videos", tags=["videos"])

# TODO: POST / (create video - submit URL)
# TODO: GET / (list videos)
# TODO: GET /{id} (get video detail)
# TODO: PATCH /{id}/timestamps (update timestamps)
# TODO: POST /{id}/publish (publish video)
