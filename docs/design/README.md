# Design

Esta pasta guarda a exportação do Claude Design que originou o redesign v2 do frontend.

Tela principal: `Quick Sermon - Redesign.html`. Abra o arquivo direto no navegador; ele referencia `components/` e `uploads/` por caminho relativo.

Os tokens de cor e tipografia do design system em `frontend/app/globals.css` vieram de `components/tokens.jsx`.
